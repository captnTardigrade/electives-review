<%- layout("layouts/boilerplate") %>
    <%- include("partials/flash") %>
        <link rel="stylesheet" href="/styles/branch_colors.css">
        <div class="row justify-content-center">
            <% for(let i of categories) { %>
                <a href="/electives/branch/<%= i %>" class="card col-md-3 col-4 mx-1 my-2 p-3 p-md-4 text-center branch-<%= i %> ">
                    <div class="card-body">
                        <%= i %>
                    </div>
                </a>
                <% } %>
        </div>
        </div>  