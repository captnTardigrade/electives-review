<%- layout("layouts/boilerplate") %>
<link rel="stylesheet" href="/styles/branch_colors.css">
    <div class="row justify-content-center">
            <% for(let i of categories) { %>
                <a href="/electives/branch/<%= i %>" class="card col-3 mx-2 my-3 p-4 text-center branch-<%= i %> ">
                    <div class="card-body">
                        <%= i %>
                    </div>
                </a>
                <% } %>
        </div>
    </div>